# CHANGELOG

## 2026-08-17

- 微应用试用状态检查改为使用静态状态接口返回的完整 `respoUrl`，避免丢失制品订单查询参数。
- 应用详情页打开制品应用时重新请求面板制品 info 配置接口，试用到期校验不再依赖前端文件下载接口。
- 顶部菜单微应用及网关插件微应用配置页同步请求制品 info，确保所有制品安装入口都能检测试用到期。
- 试用到期弹框统一引导用户通过应用菜单“授权与续费”购买，不再直接跳转制品市场地址。
- 运行期间静态资源或微应用请求遇到 `ZPK_TRIAL_EXPIRED` 时由全局请求拦截器提示试用到期并引导续费。

## 2026-08-17

- 制品安装及依赖检查遇到 `ZPK_TRIAL_EXPIRED` 时显示试用到期弹窗，并提供跳转续费入口。

## 2026-08-10

- 新增宿主级 `panelProxy` 并注入应用详情、顶部应用和网关插件三类 Wujie 微应用容器；代理固定访问同源 `/panel-api/v1/`、自动覆盖当前面板 Token，并拒绝跨域地址和目录穿越。用于微应用安全复用主面板 API，避免错误借用依赖 backend URL 的 `microappProxy`。影响模块：微应用宿主请求协议。验证：待执行类型检查和生产构建。
- 验证：`npm run build` 生产构建通过；`npm run type:check` 仍被仓库已有的 `$popupContainer` 类型、缺失 `route-listener`、`NodeJS` 命名空间等存量错误阻塞，本次修改文件未产生新增类型错误。
- 新增仓库技能 `w7panel-microapp-host-api`，说明 `panelProxy`、`microappProxy`、`k8sproxy`、`runningFirstPod`、`podShell` 与 `loginCloud` 的选择规则、返回类型、错误诊断和安全边界，并提供 Traffic、CKM 监控、MySQL 工作负载及普通微应用后端四类接入示例。影响模块：`skills/` 开发指引。验证：`quick_validate.py` 校验通过。

## 2026-08-07

- 统计分析图表公共组件（`statistics-analysis-charts`）的分组选择器从图表顶部移至左侧：Tab 样式改为左侧竖排 `a-tabs`，按钮组样式改为纵向按钮组；Cluster 集群监控、主机指标、Cilium 监控与应用运行状态四个面板统一生效，请求趋势页（单组、无选择器）不受影响；≤900px 时回退为纵向堆叠。
- 影响模块：统计分析图表公共组件及其所有调用方（Cluster 监控面板、应用运行状态、请求趋势）。
- 验证：`npm run build` 生产构建通过。
- 调整：按评审意见，图表分组选择器不再使用左侧竖排结构，改回顶部**一行横向展示**（Tab 或按钮组单行排列，按钮组左对齐），图表网格位于其下方；影响面板与验证同上。
- 调整：图表标题只在分组内**多于 1 个图**时显示（如主机指标的硬盘I/O、网络I/O、硬盘读写、网络流量两组图并排时保留“磁盘读取 IOPS”等标题便于区分）；单图组不再显示与 tab 重复的标题，请求趋势页（无 tab）标题保留。
- 修复：`ol-charts` 的 `showTitle` prop 此前以对象形式混入字符串数组 props（Vue 3 数组语法仅支持字符串），运行时在 props 规范化阶段抛 `str.replace is not a function`，导致 Cluster 监控面板图表全部渲染失败；已恢复纯字符串数组 props，并以组件内计算属性实现“未传时默认显示标题、显式传 false 才隐藏”的默认值逻辑。验证：`npm run build` 生产构建通过。
- 调整：应用运行状态（Pod指标）页的时间选择从图表内部移至「Pod指标」标题行（顶部工具栏新增时间范围选择与步长选择），图表区域填充满剩余高度；图表内不再显示重复的时间/步长控件。
- 验证：`npm run build` 生产构建通过。
- 调整：统计分析页（网关 → 统计分析 / 请求趋势）图表顶部的指标选择器（请求数/流量/带宽/状态码占比/状态码次数）从图表头部右侧控件区移至图表面板顶部左侧，与其它监控面板的选择器位置保持一致。
- 验证：`npm run build` 生产构建通过。

## 2026-08-05

- 新增项目开发规则：此后每次修改代码、配置、测试或文档，都必须追加更新本文件。
- 影响模块：项目开发与提交流程。
- 验证：已确认 `AGENTS.md` 包含追加写入和提交前检查要求。
- 修复应用详情点击“应用直达”时重复加载 AppGroup 导致的页面闪烁；普通应用菜单移至“运行状态”下方，Helm 应用保持在“域名管理”下方，并补全返回普通应用菜单所需的工作负载路由参数。
- 移除域名管理中“应用直达”的“游客访问”配置；新建或修改应用直达规则时统一要求登录访问。
- 修复从 Helm 应用直达返回域名管理时菜单类型未同步的问题，同时保持切换过程不重新加载 AppGroup。
- 将“应用直达”入口限制在 AppGroup 概览层级；工作负载子应用和微应用页面不再显示该入口。
- 修复仅存在占位 MicroApp CR、但没有 `thirdparty_cd` 菜单绑定的 AppGroup 仍显示“应用直达”的问题。

## 2026-08-06

- 存储管理中的“扩容完成”状态改为仅展示 30 秒，到期后自动隐藏；后端 PVC 成功注解保持不变。
- 统一 Helm 与普通 AppGroup 的“应用直达”菜单判断，修复直接进入 MicroApp 页面时必须先切换到应用详情或域名管理才显示的问题。
- 修复原本未绑定的存储分区扩容期间误显示“绑定中”的问题；绑定状态在任务进行中显示“扩容中”，完成后恢复实际状态。
- 新增数据组驱动的统计分析图表公共组件，Cluster 主机指标与应用运行状态共用 Tab、图表网格和时间查询交互；已完成前端构建验证。
- 统计分析的请求趋势改为使用公共图表分组容器，与 Cluster 面板统一图表外壳和响应式布局；已完成前端构建验证。
- Cluster 主机指标与应用运行状态的指标选择器改为请求趋势同款按钮组；已完成前端构建验证。
- Cluster 集群监控与 Cilium 监控的指标选择器改为请求趋势同款按钮组，并统一使用公共图表分组组件；已完成前端构建验证。
- 主机指标的节点选择器移至时间选择器左侧，保持各监控面板的控制区布局一致；已完成前端构建验证。

## 2026-08-07

- 统计分析的请求趋势指标 Tab 移至标题下方左侧，并移除时间颗粒度说明文案；已完成前端构建验证。
- 请求趋势图表移除重复的“请求趋势”标题，避免与指标 Tab 名称重复；已完成前端构建验证。
- 请求趋势时间筛选移至指标 Tab 同一行，Tab 保持左侧、时间选择器靠右；已完成前端构建验证。
- 恢复请求趋势图表标题，同时保留时间选择器与指标 Tab 同行的布局；已完成前端构建验证。
- Cluster 集群监控、主机指标和 Cilium 监控的指标 Tab 移至标题行，右侧时间与步长选择器位置保持不变；已完成前端构建验证。
- Cluster 监控面板调整为标题独占一行，图表 Tab 与时间选择器统一置于标题下方同一行；已完成前端构建验证。
- 主机指标的节点选择从按钮组改为下拉选择，位置保持在时间选择器左侧；已完成前端构建验证。
- 请求趋势的时间选择器移回命名空间筛选行，指标 Tab 行仅保留指标切换；已完成前端构建验证。
- 主机指标的硬盘与网络双图分组移除图表标题，避免与指标 Tab 重复；已完成前端构建验证。
- 修复主机双图隐藏标题后仍保留标题区空白的问题；已完成前端构建验证。
- 个人中心绑定云端账号成功后刷新页面，确保绑定状态和全局用户信息同步；已完成前端构建验证。

## 2026-08-10

- 首页 `/cluster/panel` 隐藏 Cilium 监控区块，保留其他集群与主机监控；已完成前端构建验证。

## 2026-08-13

- 应用文件管理默认使用 Pod `spec.containers` 数组第一个容器，并按容器名称匹配运行状态 ID；节点文件管理行为保持不变，已完成前端构建验证。
