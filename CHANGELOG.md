# CHANGELOG

## 2026-09-22

- MicroApp 聚合新增通用 `presentation-key`、`presentation-mode` 展示协议；`multiple` 保留同类全部入口，`singleton` 按现有 MicroApp 显示顺序只保留同类第一个入口，未声明协议的资源保持原行为。
- 影响模块：应用详情、顶部 MicroApp 聚合及通用资源元数据。
- 验证：`LOCAL_MOCK=true` Vite 生产构建与 `git diff --check` 通过；类型检查仍受仓库现有 TypeScript 与 `@types/node` 语法版本不兼容阻断。

- 应用列表删除操作增加单行 loading 状态，删除期间阻止重复提交，并在成功或失败后恢复操作状态。
- 影响模块：应用管理列表。
- 验证：`LOCAL_MOCK=true npm run build` 与 `git diff --check` 通过。
- 应用列表删除不再轮询等待 AppGroup 实际消失，依赖应用与目标应用的删除请求改为并发提交；Wujie 卸载仍等待后台清理完成，确保关联订单刷新时状态一致。
- 影响模块：AppGroup 通用卸载流程、应用管理列表、Wujie 制品市场卸载。
- 验证：`LOCAL_MOCK=true npm run build` 与 `git diff --check` 通过。

## 2026-09-21

- 新增独立运维 Copilot 页面：通过服务端 OpenAI 兼容接口流式生成受限 OpenUI Lang 诊断卡片；资源变更先 dry-run，再由用户确认执行。
- 影响模块：集群管理、OpenUI Vue 渲染与面板 API。
- 验证：`npm run build`、OpenUI Lang 解析自检。

- 集群概览的 CKM/CVM 请求判断改为读取后端 `/app-info` 返回的 `isSubCluster`，不再依赖 `/k3k/info` 的用户标记。
- 影响模块：集群概览的系统信息与资源信息展示。
- 验证：`npm run build`。

- Helm 下载包上传改用与构建镜像一致的认证分片上传和短时 `download-ticket` 流程，移除旧 `/s3bucket` 上传路径，避免两套上传行为不一致。
- 影响模块：应用列表 Helm 创建。
- 验证：`npm run build` 与 `git diff --check`。
- Wujie 通用宿主 handles 新增关联应用自动升级检测、前往面板升级和卸载能力；升级检测复用应用列表的版本检查接口，仅在可升级时显示入口，卸载复用面板 AppGroup 删除流程，关联应用身份由订单关系业务层判定。
- 影响模块：Wujie MicroApp 宿主能力、制品市场关联插件操作。
- 验证：`LOCAL_MOCK=true` 生产构建通过，`git diff --check` 通过；类型检查受项目现有 pnpm 依赖脚本批准策略及 TypeScript/`@types/node` 版本冲突阻断。
- ZPK 安装请求根据启用模块提交依赖 Release，供后端写入 AppGroup 通用依赖关系。
- 传统应用通过 `w7.cc/depends-<releaseName>` 反查插件 AppGroup，并聚合插件自身的 MicroApp 菜单；插件 MicroApp 仍归属于插件 AppGroup。
- AppGroup 依赖反查按 namespace 和 releaseName 校验逻辑应用关系，同名 Release 重装后无需插件重新安装即可继续聚合菜单。
- 安装请求中的依赖项只提交 namespace、releaseName 和应用标识；后端对每个提交项进行强校验，不再传递可选的 `required` 状态。
- 移除复数 `w7.cc/group-names` 方案，保留单数 `w7.cc/group-name` 处理同一 Release 内多个 MicroApp。
- 顶部菜单和应用详情对依赖插件 MicroApp 的展示顺序统一增加 `200` 基数，并使用 AppGroup、MicroApp 名称稳定处理同序项；菜单生成、默认 MicroApp 和默认菜单统一使用排序结果，不修改资源原始 `w7.cc/order`。
- AppGroup 依赖反查、插件 MicroApp 聚合及 `+200` 展示排序从通用资源和菜单工具中拆为独立领域模块，顶部菜单与应用详情复用同一份已排序结果，避免页面重复排序。
- 影响模块：制品安装、应用列表、应用详情和顶部 MicroApp 容器。
- 验证：Vite 生产构建与 `git diff --check` 通过。

## 2026-09-18

- 网关插件子 MicroApp 的静态资源状态查询与下载改用 `w7.cc/group-name` 定位父 AppGroup，前端属性仍按子 MicroApp 资源名读取，确保父制品回源信息可正确缓存。
- 网关插件 MicroApp 明确拆分资源名与 AppGroup 名：`frontprops` 按 `metadata.name` 查询，注入的 `group/appgroup` 固定使用 `w7.cc/group-name`。
- 影响模块：网关插件 MicroApp 静态资源加载。
- 验证：`LOCAL_MOCK=true npm run build` 与 `git diff --check`。

## 2026-09-17

- 修复 Helm 下载包与传统代码包上传仍使用旧 `/s3bucket` 相对路径导致 401/跳转登录；统一改用认证的 `/panel-api/v1/s3bucket` 调用。
- 验证：`npm run build`。

- 修复构建镜像、Helm 上传 Chart 和微应用文件下载仍在 URL 中携带 `api-token` 的遗漏：改为统一申请短时下载票据，避免构建 Job 下载 ZIP/TGZ 时被服务端的新认证边界拒绝。
- 验证：`npm run build`。

- Helm 仓库索引改用已认证的受限制品 API，传统代码包安装改为先签发短时下载票据；前端不再拼接可长期重放的下载 JWT URL。
- 验证：待本次 `npm run build`。

- 开发代理支持通过 `W7PANEL_PROXY_URL` 指定本地源码 Server，避免 Vite 在切换本地测试端口时继续使用旧的代理配置。
- 影响模块：本地 UI/Server 联调。
- 验证：待重启 Vite 后进行 Pod Shell 联调。

- WebShell 增加固定 `w7panel-terminal` 子协议，认证凭据继续使用独立的 `w7panel-bearer.*`；Server 可明确协商终端协议，避免经 Vite/反向代理时握手无响应。
- 影响模块：容器、节点和 CKM 子面板终端握手。
- 验证：本地 Server/UI 实际 Pod Shell 握手返回 101。

- 修复容器列表和 CKM 子面板 WebShell 在移除 `w7panel_session` Cookie 后无法建立连接的问题：所有终端 WebSocket 统一从当前面板会话取 token，通过 `w7panel-bearer.*` 子协议认证，并从 URL 清除遗留 `api-token`。
- 影响模块：容器终端、节点终端与 CKM 子面板终端。
- 验证：`node --test scripts/ckm-panel-session.test.cjs`、`npm run build` 与 `git diff --check`。

- 应用列表点击应用时改用面板 `GET /microapp/:name/info` 判定 MicroApp，避免子用户因 Kubernetes RBAC 缺少 MicroApp 读取权限而无法继续跳转。
- 影响模块：应用列表详情跳转。
- 验证：`npm run build` 与 `git diff --check` 通过。

- 制品安装页中带 `module_name` 的启动参数统一按自身 `name` 匹配配置接口应用列表中的同名来源参数，不再使用 `values_text` 决定同制品应用间的参数映射。
- 影响模块：多应用 ZPK 安装参数同步与表单展示。
- 验证：`LOCAL_MOCK=true npm run build` 与 `git diff --check` 通过。

## 2026-09-14

- 修复制品安装子应用的 `PVC_NAME` 已指定 `module_name`、但 `values_text` 为空时未完整继承来源应用 PVC 的问题；空值现在按 `%PVC_NAME%` 处理，并同时写入子应用的 `envKv` 和安装选项 `pvcname`。
- 影响模块：多应用 ZPK 安装参数同步与提交。
- 验证：`LOCAL_MOCK=true npm run build` 与 `git diff --check` 通过。

## 2026-08-28

- 新增按一级及二级菜单拆分的真实点击 E2E 脚本（共 23 个入口），通过 CDP 操作菜单和安全按钮。
- 测试登录凭据仅从 `W7PANEL_USER`、`W7PANEL_PASS` 环境变量读取，不写入代码。
- 统一执行时复用同一 Chrome 页面和登录会话，避免每个菜单测试重复打开登录页。
- 菜单 E2E 新增 `W7PANEL_E2E_MUTATIONS=1` 测试环境变更模式，可操作新建/创建/删除按钮并优先取消表单。
- 新增应用管理制品市场流程测试：搜索 `whoami`、点击安装并填写随机 `*.fan.b2.sz.w7.com` 测试域名。

## 2026-08-28

- 开发环境 Vite 代理默认指向本机 W7Panel 服务端 `http://172.16.1.18:18000`，便于本地联调。

## 2026-09-10

- 应用安装抽屉新增只采集配置、不调用安装接口的 `capture` 模式，并复用普通安装的参数初始化、表单校验和请求组装流程；配置结果经 Wujie 宿主回调返回给 CKM，编辑时可从已保存的 `installOptions`、域名和发布名称回填且保持可修改，避免回填值误用外部预设参数的锁定语义。
- 采集模式不再跳转安装外部依赖、创建存储资源或在订单冲突时执行强制清除与重装，依赖由 CKM 初始化流程按拓扑顺序投递；返回结果会移除镜像仓库明文凭据以及集群 Token、面板地址等即时安装字段，避免采集过程改变现有安装状态，也避免未来安装配置携带管理面板会话数据。
- 影响模块：ZPK 应用安装表单、应用安装抽屉与 Wujie 微应用宿主能力。
- 验证：`npm run build` 生产构建与 `git diff --check` 通过。

## 2026-09-09

- 修复应用详情和顶部菜单在同一 AppGroup 包含多个 MicroApp 时，Wujie `appgroup/group` 被错误注入为当前 MicroApp 资源名的问题；两个字段现固定使用真实 AppGroup 名，并新增 `microappName` 标识当前 MicroApp。
- iframe `proxy_request` 的 `${system.group}` 同步使用真实 AppGroup 名。
- 影响模块：应用详情、顶部微应用、Wujie 宿主 props 协议。
- 验证：`LOCAL_MOCK=true npm run build` 生产构建与 `git diff --check` 通过。

## 2026-09-07

- 应用详情左侧菜单除兼容读取与 AppGroup 同名的 MicroApp 外，会按 `w7.cc/group-name` 定向读取并展示同组全部 MicroApp；切换菜单时同步切换对应前端包和运行配置。
- 影响模块：应用详情 MicroApp 菜单与前端包加载。
- 验证：前端生产构建与 `git diff --check`。
- 多应用 ZPK 安装页统一按启动参数 `module_name` 解析普通参数和 `PVC_NAME`，优先读取当前安装配置中的应用，再查询已安装的外部依赖。
- 修正依赖参数名忽略大小写导致的误匹配，以及存储容量依赖值重复追加 `Gi` 的问题。
- 影响模块：ZPK 安装配置表单。
- 验证：前端生产构建与 `git diff --check`。

## 2026-09-03

- 容器列表展开或收起时获取 Pod 指标不再携带 `local=1` 查询参数。
- 影响模块：应用详情容器列表 metrics 请求。
- 验证：已检查展开触发的请求参数并通过 `git diff --check`。

## 2026-08-31

- 应用详情 Pod 列表展开行获取指标时移除 `local=1` 查询参数，避免展开操作携带本地集群限定参数。
- 影响模块：应用详情 Pod 列表指标请求。
- 验证：已检查展开事件触发的 metrics 请求参数。

## 2026-08-26

- 域名解析提示组件改用后端 `/panel-api/v1/zpk/domain-parse` 接口，统一读取主集群域名解析配置。
- 影响模块：应用域名管理页面。
- 验证：前端生产构建与 `git diff --check`。

## 2026-08-24

- 镜像管理页新增 BuildImage CRD 构建任务列表，展示任务状态、重试进度、目标镜像、完成时间和失败原因，并自动刷新任务状态。
- 镜像构建任务改为通过“构建镜像”旁的按钮在右侧抽屉中查看，关闭抽屉后停止任务列表刷新。
- 镜像构建任务入口使用主按钮样式，任务列表不再展示构建源列。
- 优化镜像构建任务抽屉宽度与表格列，将任务和目标镜像合并展示，减少列表拥挤。
- 加宽任务抽屉和操作列，避免任务操作图标换行。
- 镜像构建任务的原因列改为中文展示，未知失败原因统一显示“构建失败”并保留原始状态码提示。

## 2026-08-19

- 新增 `helm-cnb-publish` Codex 技能，封装使用 `CNB_TOKEN` 登录并将 Helm Chart 推送至 CNB OCI 制品库的同名与非同名仓库流程。
- 影响模块：个人 Codex 技能 `/home/afan/.codex/skills/helm-cnb-publish`。
- 验证：已完成静态结构检查与 `git diff --check`；`quick_validate.py` 因环境缺少 Python `yaml` 模块未能启动。

## 2026-08-19

- 移除网关管理下的统计分析菜单及 `/gateway/traffic` 路由，保留统计分析页面源码和公共图表组件供其他页面复用。
- 影响模块：网关管理路由与左侧导航。
- 验证：待执行前端生产构建。

## 2026-08-19

- 修复应用详情左侧菜单层级：应用直达仅保留在 AppGroup 资源概览下，子应用详情不再重复展示。
- 影响模块：应用详情与资源概览左侧导航。
- 验证：`npm run build` 生产构建通过。

## 2026-08-13

- 存储管理扩容进行中的状态优先显示后端返回的阶段说明，便于区分“正在重启关联 Pod”等实际等待条件；后端修复 Deployment 重启后 Pod 名称变化导致状态无法完成的问题。
- 影响模块：存储管理 PVC 扩容状态展示。
- 验证：与后端扩容状态字段兼容，待生产构建验证。
## 2026-08-17

- 微应用试用状态检查改为使用静态状态接口返回的完整 `respoUrl`，避免丢失制品订单查询参数。
- 应用详情页打开制品应用时重新请求面板制品 info 配置接口，试用到期校验不再依赖前端文件下载接口。
- 顶部菜单微应用及网关插件微应用配置页同步请求制品 info，确保所有制品安装入口都能检测试用到期。
- 试用到期弹框统一引导用户通过应用菜单“授权与续费”购买，不再直接跳转制品市场地址。
- 运行期间静态资源或微应用请求遇到 `ZPK_TRIAL_EXPIRED` 时由全局请求拦截器提示试用到期并引导续费。

## 2026-08-17

- 制品安装及依赖检查遇到 `ZPK_TRIAL_EXPIRED` 时显示试用到期弹窗，并提供跳转续费入口。

## 2026-08-10

- 新增宿主级 `panelProxy` 并注入应用详情、顶部应用和网关插件三类 Wujie 微应用容器；代理固定访问同源 `/panel-api/v1/`、自动覆盖当前面板 Token，并拒绝跨域地址和目录穿越。用于微应用安全复用主面板 API，避免错误借用依赖 backend URL 的 `microappProxy`。影响模块：微应用宿主请求协议。验证：待执行类型检查和生产构建。
- 验证：`npm run build` 生产构建通过；`npm run type:check` 仍被仓库已有的 `$popupContainer` 类型、缺失 `route-listener`、`NodeJS` 命名空间等存量错误阻塞，本次修改文件未产生新增类型错误。
- 新增仓库技能 `w7panel-microapp-host-api`，说明 `panelProxy`、`microappProxy`、`k8sproxy`、`runningFirstPod`、`podShell` 与 `loginCloud` 的选择规则、返回类型、错误诊断和安全边界，并提供 Traffic、CKM 监控、MySQL 工作负载及普通微应用后端四类接入示例。影响模块：`skills/` 开发指引。验证：`quick_validate.py` 校验通过。

## 2026-08-07

- 统计分析图表公共组件（`statistics-analysis-charts`）的分组选择器从图表顶部移至左侧：Tab 样式改为左侧竖排 `a-tabs`，按钮组样式改为纵向按钮组；Cluster 集群监控、主机指标、Cilium 监控与应用运行状态四个面板统一生效，请求趋势页（单组、无选择器）不受影响；≤900px 时回退为纵向堆叠。
- 影响模块：统计分析图表公共组件及其所有调用方（Cluster 监控面板、应用运行状态、请求趋势）。
- 验证：`npm run build` 生产构建通过。
- 调整：按评审意见，图表分组选择器不再使用左侧竖排结构，改回顶部**一行横向展示**（Tab 或按钮组单行排列，按钮组左对齐），图表网格位于其下方；影响面板与验证同上。
- 调整：图表标题只在分组内**多于 1 个图**时显示（如主机指标的硬盘I/O、网络I/O、硬盘读写、网络流量两组图并排时保留“磁盘读取 IOPS”等标题便于区分）；单图组不再显示与 tab 重复的标题，请求趋势页（无 tab）标题保留。
- 修复：`ol-charts` 的 `showTitle` prop 此前以对象形式混入字符串数组 props（Vue 3 数组语法仅支持字符串），运行时在 props 规范化阶段抛 `str.replace is not a function`，导致 Cluster 监控面板图表全部渲染失败；已恢复纯字符串数组 props，并以组件内计算属性实现“未传时默认显示标题、显式传 false 才隐藏”的默认值逻辑。验证：`npm run build` 生产构建通过。
- 调整：应用运行状态（Pod指标）页的时间选择从图表内部移至「Pod指标」标题行（顶部工具栏新增时间范围选择与步长选择），图表区域填充满剩余高度；图表内不再显示重复的时间/步长控件。
- 验证：`npm run build` 生产构建通过。
- 调整：统计分析页（网关 → 统计分析 / 请求趋势）图表顶部的指标选择器（请求数/流量/带宽/状态码占比/状态码次数）从图表头部右侧控件区移至图表面板顶部左侧，与其它监控面板的选择器位置保持一致。
- 验证：`npm run build` 生产构建通过。

## 2026-08-05

- 新增项目开发规则：此后每次修改代码、配置、测试或文档，都必须追加更新本文件。
- 影响模块：项目开发与提交流程。
- 验证：已确认 `AGENTS.md` 包含追加写入和提交前检查要求。
- 修复应用详情点击“应用直达”时重复加载 AppGroup 导致的页面闪烁；普通应用菜单移至“运行状态”下方，Helm 应用保持在“域名管理”下方，并补全返回普通应用菜单所需的工作负载路由参数。
- 移除域名管理中“应用直达”的“游客访问”配置；新建或修改应用直达规则时统一要求登录访问。
- 修复从 Helm 应用直达返回域名管理时菜单类型未同步的问题，同时保持切换过程不重新加载 AppGroup。
- 将“应用直达”入口限制在 AppGroup 概览层级；工作负载子应用和微应用页面不再显示该入口。
- 修复仅存在占位 MicroApp CR、但没有 `thirdparty_cd` 菜单绑定的 AppGroup 仍显示“应用直达”的问题。

## 2026-08-06

- 存储管理中的“扩容完成”状态改为仅展示 30 秒，到期后自动隐藏；后端 PVC 成功注解保持不变。
- 统一 Helm 与普通 AppGroup 的“应用直达”菜单判断，修复直接进入 MicroApp 页面时必须先切换到应用详情或域名管理才显示的问题。
- 修复原本未绑定的存储分区扩容期间误显示“绑定中”的问题；绑定状态在任务进行中显示“扩容中”，完成后恢复实际状态。
- 新增数据组驱动的统计分析图表公共组件，Cluster 主机指标与应用运行状态共用 Tab、图表网格和时间查询交互；已完成前端构建验证。
- 统计分析的请求趋势改为使用公共图表分组容器，与 Cluster 面板统一图表外壳和响应式布局；已完成前端构建验证。
- Cluster 主机指标与应用运行状态的指标选择器改为请求趋势同款按钮组；已完成前端构建验证。
- Cluster 集群监控与 Cilium 监控的指标选择器改为请求趋势同款按钮组，并统一使用公共图表分组组件；已完成前端构建验证。
- 主机指标的节点选择器移至时间选择器左侧，保持各监控面板的控制区布局一致；已完成前端构建验证。

## 2026-08-07

- 统计分析的请求趋势指标 Tab 移至标题下方左侧，并移除时间颗粒度说明文案；已完成前端构建验证。
- 请求趋势图表移除重复的“请求趋势”标题，避免与指标 Tab 名称重复；已完成前端构建验证。
- 请求趋势时间筛选移至指标 Tab 同一行，Tab 保持左侧、时间选择器靠右；已完成前端构建验证。
- 恢复请求趋势图表标题，同时保留时间选择器与指标 Tab 同行的布局；已完成前端构建验证。
- Cluster 集群监控、主机指标和 Cilium 监控的指标 Tab 移至标题行，右侧时间与步长选择器位置保持不变；已完成前端构建验证。
- Cluster 监控面板调整为标题独占一行，图表 Tab 与时间选择器统一置于标题下方同一行；已完成前端构建验证。
- 主机指标的节点选择从按钮组改为下拉选择，位置保持在时间选择器左侧；已完成前端构建验证。
- 请求趋势的时间选择器移回命名空间筛选行，指标 Tab 行仅保留指标切换；已完成前端构建验证。
- 主机指标的硬盘与网络双图分组移除图表标题，避免与指标 Tab 重复；已完成前端构建验证。
- 修复主机双图隐藏标题后仍保留标题区空白的问题；已完成前端构建验证。
- 个人中心绑定云端账号成功后刷新页面，确保绑定状态和全局用户信息同步；已完成前端构建验证。

## 2026-08-10

- 首页 `/cluster/panel` 隐藏 Cilium 监控区块，保留其他集群与主机监控；已完成前端构建验证。

## 2026-08-13

- 应用文件管理默认使用 Pod `spec.containers` 数组第一个容器，并按容器名称匹配运行状态 ID；节点文件管理行为保持不变，已完成前端构建验证。
## 2026-08-24

- 依赖应用安装完成后重新获取 zpk 安装配置，确保 market 回写的实际 release name 被传入主应用 Helm values。

## 2026-08-25

- 依赖安装抽屉支持接收并锁定制品依赖声明的启动参数覆盖值，传统应用安装环境依赖时会自动使用指定的 `IMAGE_VERSION`。
- 影响模块：ZPK 应用依赖检测与嵌套安装表单。
- 验证：`npm run build` 生产构建通过。

## 2026-08-26

- 个人中心账号页新增“注册集群”入口，跳转复用现有云端注册流程；已完成前端构建验证。
- 云端应用商店未绑定账号时新增“去绑定”入口，统一跳转个人中心账号页；已完成前端构建验证。
- 从个人中心进入注册集群后，注册成功返回 `/person/account`；系统管理入口仍返回 `/system/cloud`；已完成前端构建验证。
- 个人中心“注册集群”改为不判断绑定状态，直接请求 `register-to-console`，成功后刷新账号信息；已完成前端构建验证。
- 个人中心将“注册集群”热点移至绑定账号旁，并增加集群存在提示，移除独立注册集群行；已完成前端构建验证。
- 个人中心注册提示改为问号图标悬浮提示，并将“注册集群”保留为单行小字热点；已完成前端构建验证。
- 个人中心注册提示调整为头像底部对齐，避免悬浮图标出现在头像中间；已完成前端构建验证。
- 注册集群回跳逻辑支持携带站内返回地址，个人中心发起的注册完成后返回账号页；已完成前端构建验证。
- API 请求将面板会话与 Kubernetes 凭据分离，仅调整 `/k8s-proxy` 原生 Kubernetes API 的凭据传递，暂不迁移集群业务接口。

## 2026-09-07

- 为 CKM 提供 `openCkmPanel`，创建并按目标隔离保存子面板会话；子面板优先读取专用会话，不再误用宿主 token、权限缓存或 refresh token。
- 子会话 401 仅重新授权目标 CKM 并重试一次，失败返回 CKM，不刷新或清除主账号。终端使用专用 WebSocket 子协议，子面板文件下载使用认证请求，避免 token 出现在 URL。
- 验证：会话存储隔离、目标切换、旧会话续期拒绝和 WebSocket 凭据测试通过；远程 Chrome 模拟接口验证目标账号、401 换取新会话及主账号凭据保留。生产构建通过，未部署。

## 2026-09-10

- 新增 `make dev` 本地 UI 联调入口，默认端口 8011；开发代理保持指向本机 w7panel-server 的 18000 端口。已用 `make -n dev` 验证命令展开。

## 2026-09-10

- 补齐网关管理“统计分析”路由 `/gateway/traffic`，复用现有流量统计页面，修复静态菜单配置指向不存在页面的问题；待浏览器联调验证。

- 按产品约定撤销上述统计分析路由：该入口处于有意停用状态，保持不在网关路由中注册。
2026-09-14: Updated repository ignore rules to retain only `.mcp.json`, `opencode.jsonc`, `AGENTS.md`, and `.gitignore` among untracked files; verified with Git ignore checks.
## 2026-09-15

- 独立子集群入口移除集群初始化、资源购买、超卖和 CVM 资源页面；登录成功后直接进入集群概览。
- 保留 CKM 管理及 K3K 配置 CRD 的前端配置入口。
## 2026-09-15

- 根路径 `/` 默认跳转至集群概览 `/cluster/panel`；未登录用户仍由路由守卫进入登录页。

## 2026-09-15

- 本地开发代理改为 `172.16.1.3:18000`，用于本机 server 联调。

## 2026-09-17

- CKM 微应用宿主仅通过 Panel API 查询唯一的 MicroApp，移除加载菜单时对 MicroApp CRD 的两条 `k8s-proxy` 请求。
- 登录页支持 `?panellogin=1`：OIDC 已启用的子集群会自动进入既有 OIDC 授权流程。
- 修复 OIDC 回调仅保存 token 而未初始化 `k3k/info`：普通用户现按角色进入 CKM，缺失本地 K8s 信息时不再导致导航与菜单读取 role/ckm-name 发生空值崩溃。

## 2026-09-20

- 统一应用详情页与 root 级应用的左侧菜单样式：端名称移除图标和左侧间距；应用名称改为 `var(--color-text-3)`、13px 的不可折叠静态标题并默认展示菜单；各层级图标和文字取消二级缩进、保持对齐。影响模块：微应用菜单项、应用详情侧栏、root 级应用侧栏。
- 验证：`npm run build` 生产构建通过；`npm run type:check` 仍被仓库已有的缺失 `route-listener`、`replaceAll` 编译目标及其他存量类型错误阻塞，本次修改文件未产生新增类型错误。
- 调整：根据本地页面实测，端名称移除图标与额外外边距后保留基础内边距，使标题与下方应用、菜单的图标列对齐；再次执行 `npm run build` 验证通过。
- 调整：微应用标题与各级菜单项统一使用 32px 固定图标列，图标本体约束为 16px，文字起点不再依赖 SVG 或字体图标自身宽度与 margin；`npm run build` 验证通过。
- 验证：`npm run build` 通过，`git diff --check` 通过；`npm run type:check` 仍仅包含仓库已有类型错误，本次修改文件没有新增报错。
- 更正：root 级应用菜单实际由 `views/topapp/micro.vue` 引用 `components/topapp-menu.vue` 渲染；此前普通文件搜索受忽略规则影响漏掉该引用，现已按真实调用链同步端名称、静态应用标题、零层级缩进和 32px 固定图标列样式。
- 验证：root 级应用菜单调整后 `npm run build` 与 `git diff --check` 通过；`npm run type:check` 仍仅报告仓库已有问题，本次修改文件没有新增类型错误。
- 调整：应用详情路由进入后由页面菜单接管一级侧栏，系统菜单不再作为外层一级菜单保留；桌面端支持折叠，移动端通过导航栏按钮打开应用菜单，并通过面包屑返回应用列表或应用首页。影响模块：默认布局、应用路由、应用详情侧栏。
- 修复：微应用二级及更深层菜单不再显示图标，按系统菜单的 34px 层级缩进展示；应用详情面包屑统一使用应用展示名称并携带完整路由参数，修复从面包屑进入后显示应用标识的问题。验证：`npm run build` 和 `git diff --check` 通过；`npm run type:check` 仍仅包含仓库已有错误，本次文件无新增类型报错。
- 修复：应用详情页的动态一级菜单容器改为直接使用 Arco `LayoutSider` / `Drawer` 组件对象，避免 Helm 详情页中菜单内容已生成但侧栏不可见。影响模块：应用详情一级侧栏；`npm run build` 生产构建通过。
- 2026-09-20 更正：Helm 与普通应用详情页左侧菜单不可见的根因是侧栏 `height: 100%` 解析为 0 且菜单绝对定位无法撑开容器；现撤回无关的 Arco 组件对象改动，改由横向布局拉伸侧栏，并用 flex 链路承载菜单滚动区域。影响模块：应用详情一级侧栏；`npm run build` 与 `git diff --check` 验证通过。
- 2026-09-21 修复：应用详情面包屑中的应用名称链接与应用列表入口统一采用“微应用、Helm、普通工作负载”的落地页优先级，避免跳入 Helm 或微应用内部的首个工作负载；应用展示名称统一优先读取 `spec.title`，兼容旧版标题注解。影响模块：应用详情面包屑；验证结果见本次构建检查。
- 2026-09-21 验证：上述应用详情面包屑修复已通过 `npm run build` 和 `git diff --check`；浏览器已确认修复前的错误跳转路径，修复后的最终点击回归因用户接管浏览器而停止。
- 2026-09-21 调整：恢复应用详情面包屑中应用名称原有的跳转行为，点击后仍进入当前或首个具体工作负载详情；仅保留应用名称统一读取 `spec.title` 的修复，避免详情页显示应用标识。影响模块：应用详情面包屑；验证结果见本次构建检查。
- 2026-09-21 验证：恢复面包屑原跳转行为后，`npm run build` 与 `git diff --check` 均通过。
- 2026-09-21 修复：应用详情页改用与导航栏、微应用嵌入态匹配的可视区最小高度，并由内部布局继承，避免 Helm 等右侧内容较短时左侧应用菜单无法撑满屏幕。影响模块：应用详情一级侧栏；验证结果见本次构建检查。
- 2026-09-21 验证：上述应用详情侧栏高度修复已通过 `npm run build`、Chrome 1400×768 视口页面回归和 `git diff --check`。
- 2026-09-21 修复：`/login?panellogin=1` 遇到本地残留的失效 token 时，`k3k/info` 校验失败后会继续既有 OIDC 自动登录流程；验证：`npm run build` 与 `git diff --check`。
- 2026-09-21 调整：顶部应用入口与旧 `/appgroup` 地址统一跳转并复用应用详情页；通过顶部入口打开时隐藏右侧面包屑及左侧底部的应用详情、域名管理、应用直达等原生菜单，同时保留顶部应用选中状态和历史微应用路由参数。影响模块：顶部导航、顶部应用兼容路由、应用详情页；验证结果见本次构建检查。
- 2026-09-21 验证：上述顶部应用入口复用已通过 `npm run build` 与 `git diff --check`；Chrome 实测旧顶部地址正确转换到带 `from=topapp` 的应用详情路由，顶部入口隐藏面包屑和原生菜单，普通应用列表入口仍保持原布局。
- 2026-09-21 更正：顶部应用入口不再通过 `from=topapp` query 区分，恢复并直接复用旧 `topapp-micro` 路由渲染应用详情组件，并新增同父级 `topapp-direct` 兼容应用直达；详情页按路由 name 隐藏面包屑和原生底部菜单。影响模块：顶部导航、顶部应用路由、应用详情页。
- 2026-09-21 验证：路由 name 区分方案已通过 `npm run build`、`git diff --check` 与 Chrome 回归；旧顶部地址保持 `/appgroup/:group/micro`，应用直达进入 `/appgroup/:group/direct`，普通应用列表入口仍显示面包屑和原生菜单。
- 2026-09-21 修复：顶部应用入口隐藏原生底部菜单后，同步隐藏仅用于分隔原生菜单的左侧菜单分割线；存在真实底部业务菜单时仍保留分割线。影响模块：应用详情一级侧栏；验证结果见本次构建检查。
- 2026-09-21 修复：恢复旧顶部应用侧栏显隐规则，仅在存在多个可见角色或单角色包含多个一级菜单时显示侧栏；顶部入口按旧顺序先筛选当前用户角色，再去重并移除空角色，同时恢复桌面侧栏对全局 `menu/topMenu` 设置的判断。普通应用详情入口保持不变。影响模块：顶部应用详情侧栏；验证结果见本次构建检查。
- 2026-09-21 修复：补回旧顶部应用对 `do`、`appmicro` 及微应用标识查询参数变化的监听，浏览器前进后退、无界同步路由和旧应用直达标识变化时同步菜单选中项与微应用内容，且避免自身菜单跳转重复触发。影响模块：顶部应用微应用路由同步；验证结果见本次构建检查。
- 2026-09-21 验证：顶部应用旧侧栏显隐规则及路由参数监听补齐后，`npm run build` 与 `git diff --check` 均通过；构建仅保留项目既有的 Vue 深度选择器弃用警告。
- 2026-09-21 修复：顶部应用入口隐藏应用删除操作并补充普通详情删除权限校验；默认菜单为应用直达时在微应用初始化前完成重定向；查询参数监听按实际变化的 `do`、`appmicro` 来源同步；顶部入口根据菜单隐藏状态与应用切换栏重新计算内容内边距和高度。影响模块：顶部应用详情、应用直达、微应用路由同步与内容布局；验证结果见本次构建检查。
- 2026-09-21 验证：上述顶部应用回归修复已通过 `npm run build` 与 `git diff --check`；`npm run type:check` 仍仅报告 `route-listener` 缺失、既有 Axios 扩展字段及登录页类型等本次变更外的存量错误。
- 2026-09-21 修复：根级顶部应用切换菜单时恢复旧版 `routeChange` 单参数事件格式，仅在面板本身作为无界子应用运行时附带嵌套来源标记，避免限流应用将普通菜单切换误判为子面板事件而只更新 URL、不切换页面。影响模块：顶部应用微应用菜单路由；验证结果见本次构建及浏览器回归。
- 2026-09-21 更正：连续双向回归确认限流微应用切换页面后会丢失后续路由总线监听，因此撤回单参数事件兼容方案；顶部应用菜单改为按目标路由重新初始化无界实例，浏览器前进后退同步也采用相同策略，普通应用详情仍保留原总线路由与嵌套来源标记。影响模块：顶部应用微应用菜单路由；验证结果见本次构建及双向切换回归。
- 2026-09-21 验证：限流应用已在 Chrome 中连续完成“通用设置 → 限流管理 → 通用设置 → 限流管理”双向切换，URL 与右侧内容均同步更新；`npm run build` 和 `git diff --check` 通过，构建仅保留项目既有的 Vue 深度选择器弃用警告。
- 2026-09-21 修复：`/cluster/panel` 系统信息始终按主集群显示集群版本、创建时间和集群模式；仅子集群的集群地址显示当前面板地址，并移除不再展示的 CKM 配额请求。影响模块：集群概览系统信息。
- 2026-09-21 验证：上述集群概览系统信息调整已通过 `npm run build` 与 `git diff --check`；构建仅保留项目既有的 Vue 深度选择器弃用警告。
## 2026-09-21（AppGroup 级联卸载）

- 新增通用 AppGroup 级联卸载方法：卸载前通过 `w7.cc/depends-<releaseName>` 和 `spec.dependencies` 生成完整反向依赖顺序，先卸载依赖应用并等待 finalizer、卸载 Hook 完成，再卸载目标应用；查询失败、循环依赖、卸载失败或等待超时均停止后续删除。
- 应用列表、应用详情、网关插件页和 Wujie 卸载 handler 统一复用该方法，避免直接删除主应用后遗留关联应用。
- 验证：`LOCAL_MOCK=true ./node_modules/.bin/vite build --config ./config/vite.config.prod.ts` 通过；级联顺序、查询失败零删除和循环依赖零删除桩测试通过；`pnpm run type:check` 受仓库现有 TypeScript 4.8 与 `@types/node` 26 语法不兼容阻断。

## 2026-09-21（TSX Vue 导入修复）

- 修复 `components/menu/index.vue` 在旧版 Vue JSX 插件转换时丢失 `defineComponent`、`ref`、`computed` 等 named imports，导致菜单 chunk 加载后偶发 `defineComponent is not defined` 的问题；组件改用不会被错误移除的 Vue namespace import。
- 验证：`LOCAL_MOCK=true ./node_modules/.bin/vite build --config ./config/vite.config.prod.ts` 通过；构建产物的菜单 chunk 已使用绑定后的 Vue helper，不再包含裸 `defineComponent`、`ref`、`computed` 等调用。
